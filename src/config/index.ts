const TOKEN_KEY_LOCAL_STORAGE = 'accessToken';

const URL_BACKEND: string = import.meta.env.VITE_HOST_BAKEND;
const URL_API_BACKEND = URL_BACKEND + '/api';

export { TOKEN_KEY_LOCAL_STORAGE, URL_BACKEND, URL_API_BACKEND };
